// /vendor/menu — el editor de menú DENTRO del panel (con barra lateral).
// Es el mismo componente que el paso del wizard (/vendor/setup/menu); ese
// camino sigue igual y sin barra. Nació el 9-sep porque "Menú" tiene botón
// propio en el sidebar y un botón del panel no debe sacarte del panel.
export { default } from "../setup/menu/page";
