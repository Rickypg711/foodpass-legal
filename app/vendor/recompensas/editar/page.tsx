// /vendor/recompensas/editar — el editor de premios DENTRO del panel (con
// barra lateral). Es el mismo componente que el paso del wizard
// (/vendor/setup/recompensas); ese camino sigue igual y sin barra. Nació el
// 9-sep (orden de Ricardo, al recorrer /vendor/recompensas): el botón del
// panel sacaba al dueño del panel — el mismo problema que tuvo el menú.
export { default } from "../../setup/recompensas/page";
